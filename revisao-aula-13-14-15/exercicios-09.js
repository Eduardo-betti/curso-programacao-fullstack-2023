// Usando objetos, crie uma função que calcula a área de um retângulo com base na
// sua largura e altura:

const areaRetangulo = { largura: 1, altura: 1.9 };

function totalRetangulo() {
  const total = areaRetangulo.largura * areaRetangulo.altura;
  return total;
}
console.log(totalRetangulo());
